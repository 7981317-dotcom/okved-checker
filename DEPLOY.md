# Инструкция по деплою на Render

Пошаговая инструкция по развертыванию приложения на бесплатном хостинге Render.

## Шаг 1: Подготовка

Код уже загружен на GitHub: https://github.com/7981317-dotcom/okved-checker

## Шаг 2: Регистрация на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите "Get Started for Free"
3. Зарегистрируйтесь через GitHub (рекомендуется)

## Шаг 3: Создание Web Service

1. В дашборде Render нажмите **"New +"** → **"Web Service"**
2. Подключите GitHub репозиторий:
   - Нажмите "Connect a repository"
   - Выберите репозиторий `okved-checker`
   - Нажмите "Connect"

## Шаг 4: Настройка сервиса

Заполните настройки:

### Основные настройки:
- **Name**: `okved-checker` (или любое другое имя)
- **Region**: `Frankfurt (EU Central)` или `Oregon (US West)`
- **Branch**: `master`
- **Root Directory**: оставьте пустым
- **Runtime**: `Python 3`

### Build & Deploy настройки:
- **Build Command**:
  ```
  pip install -r requirements.txt
  ```

- **Start Command**:
  ```
  gunicorn app:app
  ```

### Instance Type:
- Выберите **"Free"** (бесплатный план)
  - 512 MB RAM
  - Автоматически засыпает после 15 минут неактивности
  - Первый запрос может быть медленным (пробуждение)

## Шаг 5: Environment Variables (опционально)

Если хотите вынести API ключи в переменные окружения:

1. В разделе "Environment Variables" добавьте:
   - `DADATA_API_KEY_1` = `6f2ce937e384d27d595fbf573188f0061a53eb34`
   - `DADATA_SECRET_KEY_1` = `02be50420911c8dc2b9f0bd2c3592d152abc0342`
   - `DADATA_API_KEY_2` = `dc17411a8a49402bd1edd914e25f055bf6b5d2e0`
   - `DADATA_SECRET_KEY_2` = `0de6422d38a9ad3ad56bf2f4dae4f87a854ec330`

2. Обновите `app.py` для чтения из переменных окружения (если нужно)

## Шаг 6: Деплой

1. Нажмите **"Create Web Service"**
2. Render автоматически:
   - Клонирует репозиторий
   - Установит зависимости
   - Запустит приложение
3. Дождитесь завершения деплоя (2-5 минут)

## Шаг 7: Получение URL

После успешного деплоя вы получите URL вида:
```
https://okved-checker.onrender.com
```

Это публичный адрес вашего приложения!

## Шаг 8: Проверка работы

1. Откройте URL в браузере
2. Загрузите тестовый Excel файл с ИНН
3. Проверьте результат

## Автоматический деплой

Render автоматически переразворачивает приложение при каждом push в ветку `master`:
```bash
git add .
git commit -m "Update feature"
git push origin master
```

## Ограничения бесплатного плана

- ⏱️ **Засыпание**: после 15 мин неактивности
- 🔄 **Пробуждение**: первый запрос занимает ~30 сек
- 💾 **RAM**: 512 MB
- ⏰ **750 часов/месяц** бесплатно (можно держать 1 сервис постоянно)

## Мониторинг

В дашборде Render доступны:
- 📊 Логи приложения
- 📈 Метрики (CPU, RAM)
- 🔄 История деплоев
- ⚙️ Настройки

## Кастомный домен (опционально)

Если хотите свой домен:
1. Settings → Custom Domain
2. Добавьте свой домен
3. Настройте DNS записи

## Troubleshooting

### Приложение не запускается
- Проверьте логи в дашборде
- Убедитесь что `requirements.txt` содержит все зависимости
- Проверьте Build Command

### Медленная работа
- Бесплатный план засыпает - это нормально
- Для постоянной работы рассмотрите платный план ($7/мес)

### Превышен лимит DaData
- Приложение автоматически переключится на второй ключ
- Проверьте логи для подтверждения

## Поддержка

- 📧 Render Support: https://render.com/docs
- 💬 GitHub Issues: https://github.com/7981317-dotcom/okved-checker/issues

---

**Готово!** Ваше приложение теперь доступно онлайн 24/7 🚀
